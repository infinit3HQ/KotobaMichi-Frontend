# Ensure a flat, npm-like node_modules layout to avoid Next.js self-require issues with pnpm on Windows
node-linker=hoisted
# Hoist most packages to the project root to reduce nested resolutions
hoist-pattern[]=*
public-hoist-pattern[]=next
public-hoist-pattern[]=@next/*
